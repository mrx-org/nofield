## Todo

-- View sum of 4D segments + func eval + json edit
-- the order of sequences should be as in the json sources
-- add seq.sound() ( wait for approved PR)
-- speedup at startup: prebundle Pyodide (custom build or pre-built wheels for pypulseq/nibabel).
-- make  a simple phantom

