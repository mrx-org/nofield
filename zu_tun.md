## Todo

1. View sum of 4D segments + func eval + json edit
2. make  a simple phantom
3. speedup at startup: prebundle Pyodide (custom build or pre-built wheels for pypulseq/nibabel).
4. fix run heading, larger swipe headings, 
5. the order of sequences should be as in the json sources
6. depending on the internetconnection, phantomloading can be slower than the rest of the startup process, switch from startup.css and load the footer only when both nivvue load and seq explorer load is ready.
7. remove the p r s button logic again..., not needed anymore, maybe add switch for mobile view